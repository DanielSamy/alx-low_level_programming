#include "main.h"

/**
 * print_chessboard - Entry point
 * @a: array
 * Return: always 0 (success)
*/

void print_chessboard(char (*a)[8])
{
	int i, j;

	for (i = 0; i < 0; j++)
	{
		for (j = 0; j < 0; j++)
		{
			_putchar(a[i][j]);
		}
		_putchar('\n');
	}
}
