#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: This C program prints the alphabet in lowercase
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char x = 'a';

	while (a <= 'z')
	{
		putchar(x);
		x++;
	}
	putchar("\n");

	return (0);
}
